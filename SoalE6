test("01101", 4)!
  int test (String s, int last) {
      if (last < 0) {
            return 0;
      }
      if (s.charAt(last) == "0") {
            return 2 * test (s, last-1);
      }
      return 1 + 2 * test(s, last-1);
}
